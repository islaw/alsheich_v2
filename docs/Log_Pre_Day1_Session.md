**תיעוד סשן ההכנה לקראת יום 1 – גרסת Bootstrap V2.7**
**תאריך:** 2025-06-24 עד 2025-06-25
**מזהה גרסה:** `v_bootstrap_v2.7`

---

### 🔹 רקע:

סשן זה נועד להשלים את כלל ההכנות המקדימות לביצוע יום 1 בפרויקט אלשיך. הסשן כלל חיבור של סביבת העבודה ל־GitHub ו־VS Code, הבהרות מתודולוגיות, עדכון נהלים ומסמכי תשתית, והנחת יסודות לעבודה שוטפת.

---

### 🧩 רכיבים שבוצעו/הושלמו:

#### 1. **סנכרון VS Code + GitHub**

* פתיחת חשבון GitHub: `islaw`
* חיבור VS Code לחשבון והגדרת `repository` פעיל (`alsheich_v2`)
* הגדרת branch פיתוח (`dev`)
* אימוץ מלא של סביבת העבודה דרך VS בלבד (ללא טרמינל Windows)
* הדרכה מלאה לביצוע `git add`, `commit`, `push`, `tag`

#### 2. **עדכון נוהלי כתיבה והוראות**

* כל פקודות bash מופרדות שורה-שורה
* הנחיות ניהול גרסאות (חובה להתחיל סשן מ־pull, לסיים ב־commit)
* עדכון קובץ `PROJECT_BOOTSTRAP_V2.7.md` בהתאם

#### 3. **ניהול קבצים ותיקיות לפרויקט**

* יצירת תקיות `day1_execution`, `engine`, `data`
* העברת קבצים בהתאם להנחיות יום 1
* שמירה ותיוג כל הקבצים ב־branch `dev`

#### 4. **הוספת נכסים תשתיתיים**

* הקובץ `סקירה השוואתית בין מודלים GPT‑Gemini‑Claude‑Mistral.md` הוסף לרשימת נכסי הליבה
* נספח תמחור מעודכן הוטמע בקובץ הראשי (`v2.7`)
* דרישה לעדכון שבועי + תאריך עדכון בראש כל מסמך

---

### 📝 משימות מתמשכות שנקבעו:

* כל מודל שיפתח סשן חדש נדרש לטעון תחילה את `PROJECT_BOOTSTRAP_V2.7.md` ולהתבסס על הנחיותיו.
* על כל קובץ מרכזי לעבור דרך ניהול גרסאות תקני (add → commit → push → tag)
* כל מסמך שכולל מידע משתנה (כגון תמחור, תוצאות בדיקות) יחויב בעדכון תדיר ובתיעוד תאריך עדכון

---

### ✅ לסיכום:

גרסה 2.7 מסמנת את סיום שלב ההכנה ומעבר לעבודה פרקטית יומיומית במסגרת יום 1. מסמך זה ייכנס כנספח במסמך התשתיתי הראשי, ויתווסף לרשימת הנכסים הקבועים של הפרויקט.

**הערה:** אין להתחיל יום חדש לפני ווידוא שהגרסה הנוכחית הוטמעה כראוי בכל סביבה רלוונטית.
