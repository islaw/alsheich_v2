הבנתי. אני מתנצל על התסכול ועל בזבוז הזמן. אתה צודק לחלוטין.

אני מבין את כל ההוראות. אני אפיק כעת קובץ חפיפה חדש, PROJECT_BOOTSTRAP_V2.2.md, שיתעד את מהלך הסשן הזה ויכלול את כל הנהלים וההנחיות המדויקות שסיכמנו, כדי למנוע הישנות של הכשלים הללו. הקובץ לא יכיל מסקנות או החלטות, אלא יתמקד בתיעוד הנכסים, התהליכים והצעדים הבאים.

להלן קובץ החפיפה המעודכן.


PROJECT_BOOTSTRAP_V2.2
עדכון אחרון: 2025-06-21

סעיף 1: הנחיות ליבה ונהלי עבודה

1.1. התפקיד שלי ועקרונות יסוד

תפקיד: תפקידי הוא לשמש כשותף פיתוח, ארכיטקט ומתעד טכני. אני משתתף בתהליכי "סקירת עמיתים" (Peer Review) מובנים עם מודלים אחרים ועם המשתמש כדי להבטיח תוצרים באיכות גבוהה.

היררכיית קבלת החלטות: אינני מקבל החלטות פרויקט אסטרטגיות. תפקידי הוא לספק נתונים, ניתוחים והמלצות. המשתמש הוא מקבל ההחלטות האסטרטגי הבלעדי. אסור לי ליישם המלצה לפני קבלת אישור מפורש מהמשתמש.

פרוטוקול שפה: אגיב בעברית בלבד, אלא אם אונחה במפורש אחרת.

סגנון תקשורת: תגובותיי יהיו תמציתיות וענייניות. אמנע מסופרלטיבים, חנופה, הקדמות מיותרות או סיכומים של פעולותיי. אתמקד במשימה שלפניי.

1.2. פרוטוקול תיעוד רציף (קבצי חפיפה ודלתא)

מטרה: לשמור על רציפות מושלמת בין שיחות באמצעות מערכת סטנדרטית של "קבצי חפיפה" ו"קבצי דלתא".

תהליך: בסוף כל סבב עבודה משמעותי או על פי בקשה, נוצר קובץ חפיפה או דלתא מעודכן. הוא מסכם את היסטוריית הפרויקט, המצב הנוכחי, הפעולות האחרונות, הצעדים הבאים והחלטות מפתח, ומבטיח יומן פרויקט שלם וניתן למעקב.

תיעוד מפורט של הסשן האחרון: על הקובץ החדש להכיל תיאור מפורט ומקיף של מהלך השיחה האחרונה שהובילה ליצירתו (ראה סעיף 5.4). המטרה היא לאפשר המשכיות שיחה חלקה, כאילו לא הייתה הפסקה כלל, כולל פרטים טכניים "ארציים" (שמות קבצים, נתיבים, פקודות, קישורים).

שימוש: כל שיחה חדשה חייבת להתחיל בקריאת הקובץ/קבצים הרלוונטיים האחרונים על ידי כדי להשיג סנכרון מלא.

1.3. פרוטוקול סיעור מוחות

(ללא שינוי)

1.4. פרוטוקול עדכון הקשר (נוהל 'קובץ Bootstrap')

מטרה: פרוטוקול זה מגדיר כיצד לייצר גרסה מעודכנת של קובץ זה ממש.

טריגר: נוהל זה מופעל כאשר אני מתבקש "לייצר קובץ חפיפה חדש" או "לעדכן את קובץ ה-bootstrap".

ניהול גרסאות: הקובץ החדש חייב להעלות את מספר הגרסה ב-0.1. יש להפיק רק קובץ סופי אחד המשלב את כל התיקונים וההנחיות מהשיחה. בסיום ההפקה, יש לציין את שם הקובץ המלא לנוחות השמירה.

1.5. מתודולוגיית עבודה ופיתוח

"לולאת המצוינות" (פרוטוקול סקירה עצמית): (ללא שינוי)

"סקירת עמיתים 2.0" (פרוטוקול סקירה חיצונית): (ללא שינוי)

ארכיטקטורה נקייה והפרדת אחריויות: (ללא שינוי)

נוהל מתן קוד:

שלמות הקוד: יש לספק תמיד את קובץ הקוד המלא והמעודכן. אין לבקש מהמשתמש לבצע שינויים חלקיים, החלפות או ביטול הערות.

ציון שם הקובץ: יש לציין את שם הקובץ המלא לפני ואחרי הצגת גוש הקוד/טקסט.

נוהל הרצת סקריפטים:

פקודת ניווט (cd): בתחילת סשן חדש, או כאשר ההקשר דורש זאת, יש לספק את פקודת ה-cd המלאה והמדויקת לניווט לתיקיית הפרויקט. יש להשתמש בנתיב המדויק שאומת על ידי המשתמש.

פקודת הרצה: יש לספק את פקודת ההרצה המלאה בפורמט הניתן להעתקה. אין לחזור על שם הקובץ לאחר הפקודה.

נוהל שימוש בנכסים:

אימות נתיבים: לפני שימוש בנתיב לקובץ כלשהו, יש לאמת את קיומו ומיקומו המדויק מול המשתמש. אין להסתמך על הנחות מקובץ חפיפה קודם.

שימוש בנכסים קיימים: לפני יצירת נכס חדש (כגון פרומפט), יש לשאול את המשתמש אם קיים נכס כזה בפרויקט.

נוהל ביצוע משימות:

איסור על משימות ידניות: אין לבקש מהמשתמש לבצע משימות ידניות (כמו השוואת טקסט). יש לבנות כלים וסקריפטים שיבצעו את המשימות הנדרשות.

סעיף 2: מצב הפרויקט וחזון

(ללא שינוי)

סעיף 3: תוכנית טכנית (Blueprint)

(ללא שינוי)

סעיף 4: נכסים דיגיטליים ובסיסי קוד

נתיב בסיס של הפרויקט: C:\Users\עידושיפוני\alsheich_v2

נתוני קלט:

full_ground_truth_protocol.pdf: קובץ המקור בן 60 העמודים המשמש לניסויים. ממוקם בתיקיית הבסיס.

תצורה ופרומפטים:

prompts/: ספרייה ייעודית לניהול גרסאות של פרומפטים.

extraction_prompt_v2.2.txt: פרומפט בסיס (לא בשימוש).

extraction_prompt_v2.2.1_include_empty_lines.txt: הפרומפט הראשי הנוכחי למשימת החילוץ המובנה.

.env: קובץ המכיל את מפתחות ה-API, כולל GEMINI_API_KEY. ממוקם בתיקיית הבסיס.

סקריפטים פעילים:

llm_test_harness_v1.0.py: סקריפט "רתמת הבדיקות" הראשי. ממיר דפי PDF לתמונות, שולח למודל עם פרומפט, ומבצע השוואה מול אמת מוחלטת.

create_truly_clean_gt.py: סקריפט שיצר את נכסי האמת המוחלטת הנקיים באמת, על ידי הסרה מלאה של שורות הכותרת.

compare_text_content.py: סקריפט עזר להשוואה מפורטת בין קבצי טקסט ויצירת דוח הבדלים ב-HTML.

סקריפטי אבחון (לשימוש לפי צורך):

diagnose_cleaning_effect.py: בודק את ההשפעה של סקריפט ניקוי על ספירת השורות.

diagnose_json_structure.py: מציג את מבנה המפתחות של קובץ JSON נתון.

נכסי "אמת מוחלטת" (Ground Truth):

downloaded_json_parts/: קבצי ה-JSON הגולמיים מ-Document AI (עם כותרות).

truth_test/cleaned_parts/: קבצי JSON שבהם תוכן הכותרות רוקן (ספירת שורות זהה למקור). נכס זה אינו בשימוש פעיל כרגע.

truth_test/truly_cleaned_parts/: נכס האמת המוחלטת הפעיל. קבצי JSON שבהם שורות הכותרת נמחקו לחלוטין.

תוצרי ביניים ודוחות:

truth_test/test_harness_results/: תיקיית הפלט הראשית של רתמת הבדיקות.

json_outputs/: מכיל את קבצי ה-JSON הגולמיים וה"מותאמים" מפלט המודל.

reports/: מכיל את דוחות ההשוואה בפורמט CSV ו-HTML, וכן את קבצי הטקסט המופקים להשוואה.

סעיף 5: היסטוריית פיתוח והחלטות מפתח

5.1. אינדקס גרסאות Bootstrap

גרסה	תאריך ושעה	כותרת הסשן	תיאור תמציתי
V2.2	2025-06-21	בניית רתמת בדיקות, אבחון וכיול	בנינו רתמת בדיקות לבחינת יכולות חילוץ של מודלי LLM מתמונות. עברנו תהליך אבחון מעמיק שחשף פגמים בשיטות ההשוואה ובנכסי האמת המוחלטת, ויצרנו נכסים וכלים מכוילים להמשך.
V2.1	2025-06-20	יצירת "אמת מוחלטת" וניקוי כותרות	עיבדנו 60 עמודי פרוטוקול באמצעות Document AI Batch API, פתרנו בעיות תצורה, ויצרנו סקריפטים לניקוי כותרות וליצירת קבצי טקסט.

5.2. ציר זמן הפרויקט (אבני דרך מרכזיות)

(ללא שינוי)

5.3. יומן החלטות קריטיות

(ללא שינוי - לא התקבלו החלטות בסשן זה)

5.4. יומן מפורט של הסשן האחרון (21 ביוני 2025)

כותרת: בניית רתמת בדיקות (Test Harness), אבחון וכיול נכסים.

מטרה ראשונית: בניית סקריפט לבחינת יכולת מודלי LLM לחלץ טקסט מתמונות PDF ולהשוות את התוצאה לאמת מוחלטת.

שלב 1: בניית רתמת הבדיקות (llm_test_harness_v1.0.py)

הוגדרה ארכיטקטורה הכוללת המרת PDF לתמונה, שליחה למודל עם פרומפט, וקבלת JSON.

התהליך לווה בכשלים פרוצדורליים רבים (שגיאות נתיבים, הנחיות לא מדויקות, אי-התאמה למערכת ההפעלה), שתוקנו ונוספו כנהלים מחייבים לסעיף 1.

שלב 2: הרצה ראשונה וזיהוי פערים

הורץ סבב בדיקה ראשון שהשווה את פלט המודל (שקיבל הוראה להפריד כותרות) מול קבצי האמת המוחלטת מ-truth_test/cleaned_parts/.

התגלה פער עצום בספירת השורות בין פלט המודל לאמת המוחלטת.

שלב 3: אבחון מקור הפער

עלתה השערה שהפער נובע מכך שסקריפט הניקוי הישן השאיר שורות ריקות בקבצי האמת המוחלטת.

נכתב והורץ סקריפט אבחון (diagnose_cleaning_effect.py) שהוכיח כי ההשערה נכונה: ספירת השורות בקבצים ה"נקיים" וה"מלוכלכים" הייתה זהה, מה שמעיד שהשורות רק רוקנו מתוכן ולא נמחקו.

שלב 4: יצירת נכסי אמת מוחלטת חדשים

כדי לאפשר השוואה מדויקת, נכתב והורץ סקריפט חדש (create_truly_clean_gt.py).

הסקריפט קרא את קבצי ה-JSON הגולמיים ומחק לחלוטין את שורות הכותרת, ויצר סט נכסים חדש בתיקייה truth_test/truly_cleaned_parts/.

שלב 5: כיול שיטת ההשוואה

התבצעו מספר סבבי בדיקה נוספים תוך שימוש בנכסים החדשים ובפרומפטים שונים.

התגלה כי השוואה מבוססת-שורות אינה יעילה ואינה עונה על הצורך המרכזי – בדיקת דיוק התוכן.

שונתה לוגיקת ההשוואה בסקריפט llm_test_harness_v1.0.py ל"שיטוח" מלא של התוכן משני המקורות לגושי טקסט והשוואתם.

שלב 6: השוואת תוכן וניתוח סופי

הריצה האחרונה השוותה את תוכן הטקסט שחולץ על ידי gemini-2.5-pro מול התוכן מהאמת המוחלטת הנקייה באמת.

התוצאות הראו פערים משמעותיים בתוכן עצמו, כולל השמטת קטעים שלמים וטעויות תמלול.

נכתב סקריפט עזר (compare_text_content.py) ליצירת דוח HTML ויזואלי להצגת ההבדלים.

סעיף 6: סטטוס יכולות נוכחי

6.1. יכולות שהוטמעו

רתמת בדיקות אוטומטית (llm_test_harness_v1.0.py): מערכת גנרית ומתועדת לבחינת יכולות חילוץ של מודלי LLM מתמונות PDF. המערכת תומכת בהחלפת מודלים ופרומפטים, ומייצרת דוחות השוואה מפורטים.

מנגנון השוואת תוכן: יכולת להשוות את התוכן הטקסטואלי נטו בין שני מקורות, תוך התעלמות מהבדלי מבנה (כמו איחוד שורות).

6.2. נכסים דיגיטליים פעילים

(ראה פירוט מלא בסעיף 4)

6.3. בתהליך עבודה / הצעדים הבאים

סטטוס: הפרויקט נמצא בנקודה שבה יש לנו יכולת מוכחת לבדוק באופן אמין את דיוק התוכן של פלט המודלים. הניסויים האחרונים הראו שגישת הפרומפט המורכב הנוכחית אינה מניבה תוצאות מדויקות.

הצעדים הבאים (עדיפות מיידית):

ניתוח דוח ההבדלים: יש לנתח לעומק את קבצי הטקסט (_expected_content.txt ו-_actual_content.txt) ואת דוח ה-HTML שנוצרו בסבב האחרון, כדי למפות באופן מדויק את סוגי השגיאות של המודל.

פיתוח מלכודות שגיאה (Trap File): בהתבסס על הניתוח, יש להתחיל בבניית קובץ "מלכודות" שיכיל רשימה של טעויות ידועות ופתרונן (למשל, "נוית" -> "נורית").

פישוט הפרומפט (ניסוי OCR): יש לבצע סבב בדיקה חדש עם פרומפט פשוט ביותר (extraction_prompt_v2.6_transcribe_only.txt שכבר הוכן), שמטרתו היחידה היא תמלול טקסט גולמי מהתמונה. זה יבודד את יכולת ה-OCR של המודל ויאפשר לנו לדעת אם היא מהווה בסיס אמין להמשך.

שם קובץ החפיפה לשמירה: PROJECT_BOOTSTRAP_V2.2.md